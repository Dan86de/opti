# Effect and Cloudflare tooling both expect a non-hoisted, strict store.
strict-peer-dependencies=false
auto-install-peers=true

# Vendored subtrees under repos/ are reference material, never workspace members.
# See pnpm-workspace.yaml for the exclusion.
