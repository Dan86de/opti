# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# test output
coverage/

# editor / os
.DS_Store
*.local

# NOTE: repos/ is deliberately NOT ignored. Vendored subtrees are committed on
# purpose so that a fresh clone gives a coding agent the reference source.
